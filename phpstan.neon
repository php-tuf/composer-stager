parameters:
    level: max
    tmpDir: var/phpstan
    paths:
        - bin/composer-stage
        - src
    exceptionRules:
        reportCheckedThrowsInGlobalScope: false
    ignoreErrors:
        -
            message: '#PHPDoc tag @throws with type [a-zA-Z0-9\|\\_]+ is not compatible with parent#'
            path: .

services:
    -
        class: \PhpTuf\ComposerStager\Tests\PHPStan\InternalAnnotationRule
        tags:
            - phpstan.rules.rule
